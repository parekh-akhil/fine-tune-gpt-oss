# Core dependencies for fine-tuning GPT OSS 20B with Unsloth
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
torch>=2.1.0
transformers>=4.36.0
datasets>=2.14.0
accelerate>=0.24.0
peft>=0.7.0
bitsandbytes>=0.41.0
trl>=0.7.4

# Data processing
pandas>=2.0.0
numpy>=1.24.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0

# Optional: Weights & Biases for experiment tracking
wandb>=0.15.0
